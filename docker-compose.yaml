services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: inventory_postgres
      POSTGRES_PASSWORD: secretInventory
      POSTGRES_DB: inventoryDB
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: